# 安全政策

## 支持范围

目前仅维护 `main` 分支的最新代码，尚未承诺版本化的安全支持周期。

## 报告漏洞

请通过网站现有联系邮箱 **wpx0428@126.com** 私下报告，主题建议为 `[websites security] 简要描述`。请提供受影响的提交、复现步骤、影响范围和建议修复方式；先脱敏，不发送真实密钥或无关个人数据。

不要在公开 Issue 或 Pull Request 中披露未修复漏洞的可利用细节。维护者会在确认问题后协调修复与披露；个人项目不承诺固定响应时限，也没有漏洞赏金计划。

## 安全边界

网站没有登录、支付、表单后端和应用数据存储。仍需关注依赖漏洞、外部链接、HTML/脚本注入以及部署凭据。所有 `VITE_*` 环境变量都应视为浏览器可见，不可用于保存秘密。

若凭据曾被提交，应先吊销或轮换，再处理 Git 历史及缓存；只删除当前文件不能撤销已泄露的凭据。

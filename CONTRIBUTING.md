# 贡献指南

欢迎提交可复现的问题、文档修正、无障碍改进和小范围修复。较大的视觉改版、新功能或架构调整，请先开 Issue 说明问题与方案，避免完成后才发现方向不一致。

## 开发流程

1. Fork 仓库并创建主题分支。
2. 按 README 使用 Node.js 24 和 `npm ci` 安装依赖。
3. 阅读 `AGENTS.md`，保留已接受的设计、文案与交互约定。历史记录中的发布授权属于维护者原任务，不是贡献者的部署许可。
4. 保持修改聚焦；改动行为时增加或调整有意义的测试。
5. 运行 `npm run build` 和 `npm test`。
6. 提交 Pull Request，说明问题、改动和验证结果。页面变化请附脱敏后的桌面与移动端截图，并检查键盘操作、减少动态效果和深层链接刷新。

`npm run test:sites` 已包含在 `npm test` 中，也可单独运行。请提交有必要的 lockfile 更新，不提交依赖目录和构建产物。

## 内容与权利

- 不提交凭据、私人草稿、客户信息、原始直播或未经同意的个人信息。
- 图片、字体及截图必须说明来源、许可证和可再分发依据。网上可以下载不等于可以随仓库分发。
- 代码和技术文档贡献按本仓库 MIT 协议提供；素材遵循各自的许可，见 `THIRD_PARTY_NOTICES.md`。
- 使用 AI 辅助时，同样需要验证结果、注明素材来源，不编造测试结论或项目效果。

## 沟通

用中文或英文均可。围绕可复现的问题讨论，尊重其他参与者；不接受骚扰、人身攻击或公开他人私人信息。安全漏洞请按 `SECURITY.md` 私下报告，不在公开 Issue 中贴利用细节。

项目由个人维护，不承诺响应时间或接受所有提议。发布版本、部署站点和调整仓库权限由维护者决定。
